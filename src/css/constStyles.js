import { StyleSheet } from 'react-native';
import ScreenUtils, { scaleSizeW, scaleSizeH } from '../util/ScreenUtils'


const styles = StyleSheet.create({
    tabBarIcon: {
        width: scaleSizeW(16),
        height: scaleSizeH(16),
        marginTop: 5,
    }
});